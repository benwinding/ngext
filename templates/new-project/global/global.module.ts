import { CommonModule } from "@angular/common";
import { NgModule } from "@angular/core";

@NgModule({
  imports: [
    CommonModule,
    // Globally accessible imports
  ],
  declarations: [],
  providers: [
    // Globally accessible providers
  ],
})
export default class GlobalModule {}
// ^^ Must be default export
